<h1>All articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %> (<%= article.title %>)</li>
  <% end %>
</ul>

<p><%= link_to "Add a article", new_article_path %></p>
